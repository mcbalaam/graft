name: release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: build linux/amd64
        run: |
          GOOS=linux GOARCH=amd64 make build
          cp graft graft-${{ github.ref_name }}-linux-amd64
          sha256sum graft-${{ github.ref_name }}-linux-amd64 > graft-${{ github.ref_name }}-linux-amd64.sha256

      - name: build linux/arm64
        run: |
          GOOS=linux GOARCH=arm64 make build
          cp graft graft-${{ github.ref_name }}-linux-arm64
          sha256sum graft-${{ github.ref_name }}-linux-arm64 > graft-${{ github.ref_name }}-linux-arm64.sha256

      - name: create release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            graft-*-linux-amd64
            graft-*-linux-arm64
            *.sha256
          generate_release_notes: true
